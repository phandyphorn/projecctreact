import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

// Everything combine in routes, so maybe index contain only App component.
const root = document.getElementById("root");
ReactDOM.render(
    <App />
, root);